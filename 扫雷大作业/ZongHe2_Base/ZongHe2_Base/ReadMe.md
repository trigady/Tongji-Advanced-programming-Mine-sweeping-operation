#编码规范

局部函数（单独cpp内调用不作跨文件调用的）请用小驼峰命名
局部变量 小驼峰

全局函数（跨cpp或者main.cpp会用到的）大驼峰
全局变量 大驼峰
注意变量名的清楚描述

#代码注释
头文件开头需要注释该文件主体内容，主要目的
函数开头需要注释该函数的功能，入参出参描述

#代码风格
所有代码分块书写，每块应不超过10行，每行应不超过160个字符
每块内容应是做相同操作的集合，每块应尽可能给予注释 
每块之间用一个空行分割（例如读文件和操作读出来的数据应该视作两个操作，放在两个块内）